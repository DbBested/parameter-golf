---
phase: 03-mixed-precision-quantization
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
requirements:
  - QUANT-02
  - QUANT-04
  - QUANT-05
autonomous: false

must_haves:
  truths:
    - "Int5 MLP quantization produces an artifact under 16,000,000 bytes"
    - "Roundtrip BPB degradation from int5 MLP is less than 0.005 vs pre-quantization BPB"
    - "3% magnitude pruning works correctly with int5 (no errors in logs)"
    - "Per-layer quantization sensitivity MSE values are logged for all weight matrices"
  artifacts: []
  key_links:
    - from: "SLURM training log"
      to: "artifact size validation"
      via: "int5_6 log lines in training output"
      pattern: "int5_6.*bytes"
    - from: "SLURM training log"
      to: "quantization sensitivity"
      via: "quant_sensitivity log lines"
      pattern: "quant_sensitivity.*mse"
---

<objective>
Validate the int5 MLP training run results: confirm artifact fits under 16MB, BPB is acceptable, and per-layer sensitivity logging works.

Purpose: The code change from Plan 03-01 is minimal (2 lines + logging), but we must confirm it produces correct results end-to-end. The artifact size is the critical gate -- Phase 2's 18.49MB must drop below 16MB. BPB degradation from int5 should be < 0.005.

Output: Validated training results confirming artifact size, BPB, and sensitivity logging.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mixed-precision-quantization/03-CONTEXT.md
@.planning/phases/03-mixed-precision-quantization/03-RESEARCH.md
@.planning/phases/03-mixed-precision-quantization/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check training job completion and extract results</name>
  <files></files>
  <read_first>
    - .planning/phases/03-mixed-precision-quantization/03-01-SUMMARY.md (to get SLURM job ID)
  </read_first>
  <action>
    1. Check if the SLURM job from Plan 03-01 has completed:
       ```bash
       squeue -u $USER  # Check if still running
       sacct -j <JOB_ID> --format=JobID,State,Elapsed,ExitCode  # Check completion status
       ```

    2. If the job is still running, wait for it. Check periodically with squeue.

    3. Once complete, find and read the training log:
       ```bash
       ls -lt logs/int5_seed1337_*.log  # Find the log file
       ```

    4. Extract key metrics from the log:
       - Search for "int5_6" lines to find artifact size
       - Search for "final_int5_6_roundtrip" to find roundtrip BPB
       - Search for "quant_sensitivity" to find per-layer MSE values
       - Search for "FAIL\|PASS" in artifact cap check from SLURM script
       - Search for training step count and wall time

    5. Also check the artifact file directly:
       ```bash
       cd /orcd/home/002/tomli/parameter_golf/repo
       ls -la final_model.int8.ptz
       stat -c%s final_model.int8.ptz
       wc -c < train_gpt.py
       ```

    6. Compile a results table with:
       | Metric | Phase 2 (int6) | Phase 3 (int5 MLP) | Delta |
       |--------|---------------|-------------------|-------|
       | Artifact bytes | 18,487,380 | ? | ? |
       | val_bpb (roundtrip) | 1.1356 | ? | ? |
       | Under 16MB cap | NO | ? | ? |
       | Steps completed | 6,974 | ? | ? |
       | Wall time | 600s | ? | ? |
  </action>
  <verify>
    - SLURM job completed with exit code 0
    - Training log exists and contains all expected metrics
    - Artifact file exists in repo/ directory
  </verify>
  <acceptance_criteria>
    - Training job completed successfully (exit code 0)
    - All metrics extracted: artifact size, BPB, sensitivity MSE, steps, wall time
    - Results table compiled comparing Phase 2 vs Phase 3
  </acceptance_criteria>
  <done>Training results are extracted and compiled into a comparison table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate training results meet phase criteria</name>
  <what-built>
    Mixed int5/int6 quantization training run. The core change was two lines in mixed_quantize_int6() to use clip_range=15 for MLP weights instead of uniform clip_range=31. Per-layer reconstruction MSE logging was added to the serialization path.
  </what-built>
  <how-to-verify>
    Review the results from Task 1:

    1. **Artifact size (CRITICAL):** Must be under 16,000,000 bytes (code + compressed model).
       - Phase 2 was 18,487,380 bytes (OVER)
       - Expected: ~15.7-15.9MB based on SOTA #1 measurements
       - If OVER 16MB: Phase 3 has FAILED -- need to investigate

    2. **BPB quality:** Roundtrip val_bpb should be close to Phase 2's 1.1356.
       - Expected degradation from int5: 0.001-0.003 BPB
       - Acceptable range: <= 1.1406 (0.005 worse than Phase 2)
       - If > 1.141: Investigate -- may need to keep MLP down-projections at int6

    3. **Per-layer sensitivity:** Check quant_sensitivity log lines.
       - MLP layers should show higher MSE than attention layers (since int5 has more quantization noise)
       - No layer should have MSE > 1e-3 (would indicate extreme sensitivity)

    4. **Pruning (QUANT-04):** Confirm no errors around magnitude pruning in logs.

    5. **Training stability:** Check for any NaN/inf warnings, gradient explosions, or unexpected early stopping.

    If all criteria pass, type "approved".
    If artifact is over 16MB, describe the failure.
    If BPB degradation exceeds 0.005, describe the issue.
  </how-to-verify>
  <resume-signal>Type "approved" if artifact is under 16MB and BPB is acceptable, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. SLURM job completed successfully
2. Artifact size < 16,000,000 bytes
3. Roundtrip val_bpb <= 1.1406 (Phase 2 + 0.005 tolerance)
4. Per-layer quant_sensitivity lines present in log
5. No training errors or instabilities
</verification>

<success_criteria>
- Artifact fits under 16MB cap (the CRITICAL gate for this phase)
- BPB degradation from int5 MLP is within acceptable range (< 0.005)
- Per-layer sensitivity data is logged and shows no extreme outliers
- 3% magnitude pruning works correctly with int5
- All three phase requirements (QUANT-02, QUANT-04, QUANT-05) validated end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/03-mixed-precision-quantization/03-02-SUMMARY.md`
</output>
