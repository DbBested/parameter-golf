---
phase: 04-differentiator-stack
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - scripts/slurm/train_phase4.sbatch
autonomous: false
requirements:
  - ARCH-05
  - ARCH-06
  - ARCH-07

must_haves:
  truths:
    - "Training completes successfully on 8xH200 within 600s wall cap"
    - "Compressed artifact (code + model) fits under 16,000,000 bytes"
    - "BPB is <= 1.145 after int5/int6 roundtrip with sliding window eval"
    - "Artifact has >= 200KB headroom under the 16MB cap (target < 15,800,000)"
  artifacts:
    - path: "repo/final_model.int8.ptz"
      provides: "Compressed model artifact"
    - path: "logs/ (phase4 run log)"
      provides: "Training log with BPB, artifact size, timing"
  key_links:
    - from: "train_gpt.py pruning at 5%"
      to: "final_model.int8.ptz size"
      via: "more zeros -> better zstd compression"
      pattern: "quantile.*0\\.05"
    - from: "train_gpt.py code size"
      to: "total artifact size"
      via: "code_bytes + model_bytes < 16000000"
      pattern: "wc -c.*train_gpt.py"
---

<objective>
Submit a SLURM training job with the optimized train_gpt.py (stripped code + 5% pruning) and validate that the artifact fits under 16MB while maintaining BPB <= 1.145.

Purpose: This is the critical validation that Phase 4's artifact size fix actually works. The code changes in Plan 04-01 (comment stripping saving ~9KB, pruning increase from 3% to 5%) should bring the artifact from 16,173,399 bytes to well under 16,000,000 bytes. We need a real training run to confirm both the size reduction and that BPB quality is maintained.

Output: Training log showing BPB and artifact size, confirming the model fits under cap.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-differentiator-stack/04-CONTEXT.md
@.planning/phases/04-differentiator-stack/04-01-SUMMARY.md
@repo/train_gpt.py
@scripts/slurm/train_phase4.sbatch
</context>

<tasks>

<task type="auto">
  <name>Task 1: Submit SLURM training job and monitor</name>
  <read_first>
    - .planning/phases/04-differentiator-stack/04-01-SUMMARY.md (confirm Plan 01 completed, get code size)
    - scripts/slurm/train_phase4.sbatch (the job script to submit)
    - repo/train_gpt.py (verify pruning is at 0.05 before submitting)
  </read_first>
  <files>scripts/slurm/train_phase4.sbatch</files>
  <action>
    **Pre-flight checks (do NOT submit until all pass):**
    1. `grep "0.05" repo/train_gpt.py | grep quantile` -- confirm pruning at 5%
    2. `wc -c repo/train_gpt.py` -- confirm under 54,000 bytes
    3. `python3 -c "import ast; ast.parse(open('repo/train_gpt.py').read()); print('OK')"` -- confirm valid

    **Submit the job:**
    ```bash
    cd /orcd/home/002/tomli/parameter_golf
    eval "$(conda shell.bash hook)"
    conda activate pgolf
    sbatch scripts/slurm/train_phase4.sbatch 1337
    ```

    Record the SLURM job ID. The job will take ~10-15 minutes (600s training + overhead).

    **Monitor:**
    Check job status with `squeue -u tomli` periodically.
    Once complete, read the log file to extract:
    - `final_int5_6_roundtrip val_bpb:` value
    - `Total submission size int5_6+zstd:` value (must be < 16,000,000)
    - Artifact size cap check result (PASS/FAIL)
    - Training time and steps completed

    **If the artifact STILL exceeds 16MB** (unlikely given ~9KB code savings + 5% pruning):
    - Check if the per-layer sensitivity logging was removed (it adds ~500 bytes to code)
    - Consider reducing BigramHash buckets from 10240 to 8192 as a fallback (change `BIGRAM_VOCAB_SIZE` default)
    - Re-run with the fix
  </action>
  <verify>
    From the training log:
    1. `grep "PASS\|FAIL" logs/slurm/*-phase4.out` -- must show PASS
    2. `grep "final_int5_6_roundtrip val_bpb" logs/*phase4*.log` -- BPB must be <= 1.145
    3. `grep "Total submission size" logs/*phase4*.log` -- must show < 16,000,000 bytes
  </verify>
  <acceptance_criteria>
    - SLURM job completed successfully (no errors)
    - Artifact total (code + compressed model) under 16,000,000 bytes
    - BPB <= 1.145 after int5/int6 roundtrip with sliding window eval
    - Training completed within 600s wall cap
  </acceptance_criteria>
  <done>Training job completed, artifact under 16MB, BPB <= 1.145</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of training results</name>
  <what-built>
    Phase 4 training run with optimized code (stripped comments, 5% pruning). The three differentiator techniques (SmearGate, U-Net skips, OrthoInit) were verified present in Plan 04-01. This run validates that the artifact now fits under 16MB while maintaining competitive BPB.
  </what-built>
  <how-to-verify>
    Review the training log output from Task 1:

    1. **Artifact size**: Confirm "Total submission size" is under 16,000,000 bytes
       - Target: < 15,800,000 bytes (200KB headroom)
       - Previous: 16,173,399 bytes (173KB OVER)

    2. **BPB quality**: Confirm "final_int5_6_roundtrip val_bpb" is <= 1.145
       - Previous Phase 3: 1.1424
       - SOTA reference: 1.1428
       - Acceptable range: 1.140 - 1.145

    3. **Training stability**: Confirm training completed without errors
       - Steps completed should be 6,000-7,000 (limited by 600s wall cap)
       - No NaN/inf in training loss

    4. **Code size**: Confirm code bytes are under 54,000
       - Previous: 62,489 bytes

    If all four checks pass, type "approved".
    If artifact is still over 16MB, describe the gap and we'll apply further fixes.
    If BPB degraded significantly (> 1.148), note the value.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Artifact under 16,000,000 bytes (PASS in SLURM log)
2. BPB <= 1.145 (from sliding window eval)
3. Training stable (no crashes, reasonable step count)
4. All three differentiators (SmearGate, U-Net, OrthoInit) contributing to the result
</verification>

<success_criteria>
- Artifact size: under 16,000,000 bytes with >= 200KB headroom
- BPB: <= 1.145 after int5/int6 roundtrip sliding window eval
- Training: completed within 600s wall cap
- Human: approved the results
</success_criteria>

<output>
After completion, create `.planning/phases/04-differentiator-stack/04-02-SUMMARY.md`
</output>
