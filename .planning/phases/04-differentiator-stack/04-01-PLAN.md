---
phase: 04-differentiator-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - repo/train_gpt.py
  - scripts/slurm/train_phase4.sbatch
autonomous: true
requirements:
  - ARCH-05
  - ARCH-06
  - ARCH-07

must_haves:
  truths:
    - "SmearGate gating mechanism exists in the model and is called in both forward() and forward_logits()"
    - "U-Net skip connections split layers into encoder/decoder halves with learned skip weights"
    - "Orthogonal initialization is applied to all weight matrices >= 64x64 in _init_weights()"
    - "Code (train_gpt.py) is under 54,000 bytes after stripping comments and docstrings"
    - "Magnitude pruning uses 5% threshold instead of 3%"
  artifacts:
    - path: "repo/train_gpt.py"
      provides: "Complete training script with all differentiators and reduced code size"
      contains: "SmearGate"
    - path: "scripts/slurm/train_phase4.sbatch"
      provides: "SLURM job script for Phase 4 validation run"
      contains: "pg_tata"
  key_links:
    - from: "SmearGate class"
      to: "GPT.forward()"
      via: "self.smear(x) call after RMSNorm"
      pattern: "self\\.smear\\("
    - from: "skip_weights parameter"
      to: "GPT.forward() decoder loop"
      via: "skip_weights[i] * skips.pop()"
      pattern: "skip_weights\\[i\\]"
    - from: "_init_weights"
      to: "nn.init.orthogonal_"
      via: "orthogonal init for large weight matrices"
      pattern: "nn\\.init\\.orthogonal_"
---

<objective>
Verify existing differentiator implementations (SmearGate, U-Net skips, OrthoInit) and fix the 173KB artifact size overage by stripping comments/docstrings from train_gpt.py and increasing magnitude pruning from 3% to 5%.

Purpose: All three ARCH requirements (05, 06, 07) were implemented in Phase 2 but not formally verified for Phase 4. The critical blocker is the artifact at 16,173,399 bytes -- 173KB over the 16MB cap. Code size reduction (~9KB from stripping 75+ comment lines) plus increased pruning (more zeros = better zstd compression) will bring the artifact under budget.

Output: A leaner train_gpt.py with verified differentiators and a SLURM script ready for validation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-differentiator-stack/04-CONTEXT.md
@.planning/phases/03-mixed-precision-quantization/03-02-SUMMARY.md
@.planning/phases/03-mixed-precision-quantization/03-VERIFICATION.md
@repo/train_gpt.py
@repo/records/track_10min_16mb/2026-03-20_10L_Int5MLP_MuonWD04_SWA50/train_gpt.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify differentiators exist and strip code to reduce artifact size</name>
  <read_first>
    - repo/train_gpt.py (full file -- the script to modify)
    - repo/records/track_10min_16mb/2026-03-20_10L_Int5MLP_MuonWD04_SWA50/train_gpt.py (SOTA reference at 52,930 bytes -- target similar size)
  </read_first>
  <files>repo/train_gpt.py</files>
  <action>
    **Step 1 -- Verify ARCH-05, ARCH-06, ARCH-07 exist (do NOT remove or change them):**

    Confirm these are present and wired:
    - ARCH-05 (SmearGate): Class `SmearGate` exists, `self.smear = SmearGate(model_dim)` in GPT.__init__, `self.smear(x)` called in both `forward()` and `forward_logits()` after RMSNorm
    - ARCH-06 (U-Net skips): `self.num_encoder_layers`, `self.num_decoder_layers`, `self.skip_weights` in GPT.__init__; encoder/decoder loop with `skips.append(x)` and `skip_weights[i] * skips.pop()` in both `forward()` and `forward_logits()`
    - ARCH-07 (OrthoInit): `nn.init.orthogonal_(module.weight, gain=1.0)` in `_init_weights()` for matrices >= 64x64

    Log verification results. If any are missing, raise an error -- do NOT proceed.

    **Step 2 -- Strip comments and docstrings to reduce code bytes:**

    The current file is 62,489 bytes with 107 comment lines. The SOTA reference is 52,930 bytes with 32 comment lines. Target: under 54,000 bytes.

    Remove:
    - The module-level docstring (lines 1-5, the "Hard stop: 1500 lines" notice -- this is informational only)
    - All `# ----` section divider lines (e.g., `# -----------------------------`)
    - All multi-line comment blocks that explain WHY code exists (e.g., "It's common for small models..." block at line 190-196)
    - All single-line comments that are purely explanatory (not functional). Keep comments that contain functional info like magic numbers or format specs.
    - All class/method docstrings (triple-quoted strings used as documentation)
    - Remove blank lines between consecutive function/class definitions where there are 2+ blank lines (reduce to 1)
    - Remove trailing whitespace

    Do NOT:
    - Remove any actual code
    - Rename variables or functions
    - Change any logic or behavior
    - Remove `CONTROL_TENSOR_NAME_PATTERNS` or `FP16_KEEP_NAME_PATTERNS` default strings
    - Remove the `# fixes applied` comment at end if present

    **Step 3 -- Increase magnitude pruning from 3% to 5%:**

    In the serialization section, change:
    ```python
    threshold = torch.quantile(param.abs().float().flatten(), 0.03)
    ```
    to:
    ```python
    threshold = torch.quantile(param.abs().float().flatten(), 0.05)
    ```

    This zeros out the smallest 5% of weights (vs 3%), creating more zeros for zstd to compress. The SOTA reference achieves 1.1428 BPB with 3% pruning -- 5% with our architecture should cost at most 0.001-0.002 BPB while saving significant compressed bytes.

    **Step 4 -- Verify final code size:**

    After all changes, check `wc -c repo/train_gpt.py`. Target: under 54,000 bytes. If still over 54,000:
    - Look for more verbose comments to strip
    - Shorten long f-string log messages by removing redundant labels
    - Remove the `per-layer quantization sensitivity` logging block (lines 1375-1392) -- it was useful for Phase 3 diagnostics but adds ~500 bytes of code and is no longer needed

    The sensitivity logging block specifically is:
    ```python
    # Per-layer quantization sensitivity (QUANT-05)
    if master_process:
        log0("=== Per-layer quantization sensitivity ===")
        for name, param in base_model.named_parameters():
            ...
        log0("=== End quantization sensitivity ===")
    ```
    This can be safely removed -- the sensitivity data was already collected in Phase 3.
  </action>
  <verify>
    Run these checks:
    1. `wc -c repo/train_gpt.py` -- must be under 54,000 bytes
    2. `wc -l repo/train_gpt.py` -- must be under 1500 lines (hard cap)
    3. `grep -c "SmearGate" repo/train_gpt.py` -- must be >= 2 (class + usage)
    4. `grep -c "skip_weights" repo/train_gpt.py` -- must be >= 3 (init + forward + forward_logits)
    5. `grep -c "orthogonal_" repo/train_gpt.py` -- must be >= 1
    6. `grep "0.05" repo/train_gpt.py` -- must show the pruning threshold line
    7. `python3 -c "import ast; ast.parse(open('repo/train_gpt.py').read()); print('Syntax OK')"` -- must print "Syntax OK"
  </verify>
  <acceptance_criteria>
    - SmearGate, U-Net skips, and OrthoInit verified present and wired in both forward paths
    - Code size reduced to under 54,000 bytes (from 62,489)
    - Magnitude pruning threshold changed from 0.03 to 0.05
    - File still parses as valid Python
    - Line count still under 1500
    - No functional behavior changed (only comments/docstrings removed and pruning threshold updated)
  </acceptance_criteria>
  <done>All three differentiators verified, code under 54,000 bytes, pruning at 5%, file syntactically valid</done>
</task>

<task type="auto">
  <name>Task 2: Create SLURM job script for Phase 4 validation run</name>
  <read_first>
    - scripts/slurm/train_int5.sbatch (template to base on)
  </read_first>
  <files>scripts/slurm/train_phase4.sbatch</files>
  <action>
    Create `scripts/slurm/train_phase4.sbatch` based on `train_int5.sbatch` with these changes:

    1. Job name: `pgolf-phase4`
    2. Output/error: `logs/slurm/%j-phase4.out` / `logs/slurm/%j-phase4.err`
    3. RUN_ID: `phase4_seed${SEED}_$(date +%Y%m%d_%H%M%S)`
    4. Echo header: `=== Phase 4 Validation: seed=$SEED run_id=$RUN_ID ===`
    5. Keep the same environment setup (miniforge, cuda, cudnn modules, conda pgolf)
    6. Keep the artifact size cap check at the end
    7. Add a line after the artifact check that prints the code byte count separately:
       `echo "=== Code bytes: $(wc -c < train_gpt.py) ==="`
    8. Keep partition as pg_tata, 8 GPUs, 30 min time limit

    The script should be identical in structure to train_int5.sbatch, just with updated naming.
  </action>
  <verify>
    1. `bash -n scripts/slurm/train_phase4.sbatch` -- no syntax errors
    2. `grep "pg_tata" scripts/slurm/train_phase4.sbatch` -- correct partition
    3. `grep "phase4" scripts/slurm/train_phase4.sbatch` -- naming present
    4. `grep "16000000" scripts/slurm/train_phase4.sbatch` -- cap check present
  </verify>
  <acceptance_criteria>
    - SLURM script exists with correct pg_tata partition and 8 GPU config
    - Artifact size cap check included
    - Script syntax is valid bash
  </acceptance_criteria>
  <done>SLURM script ready for Phase 4 validation job submission</done>
</task>

</tasks>

<verification>
1. All three ARCH requirements verified present in code
2. Code size under 54,000 bytes (significant reduction from 62,489)
3. Pruning threshold at 5%
4. SLURM script ready for validation run
5. No functional regressions -- only comment stripping and pruning threshold change
</verification>

<success_criteria>
- ARCH-05 (SmearGate): verified present, class + forward calls
- ARCH-06 (U-Net skips): verified present, encoder/decoder split + skip_weights
- ARCH-07 (OrthoInit): verified present, orthogonal_ init for large matrices
- Code size: under 54,000 bytes (saving ~8,500+ bytes)
- Pruning: 5% threshold (up from 3%)
- SLURM script: ready to submit
</success_criteria>

<output>
After completion, create `.planning/phases/04-differentiator-stack/04-01-SUMMARY.md`
</output>
